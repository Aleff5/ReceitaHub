<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <TextView
        android:id="@+id/tv_chat_message"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/chat_bubble_ai"
        android:padding="12dp"
        android:textColor="@color/white_text"
        android:textSize="16sp"
        app:layout_constraintWidth_max="wrap"
        app:layout_constraintWidth_percent="0.8"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Olá! Sou o ReceitaHub, seu assistente de culinária. Como posso ajudar?" />

    <ImageButton
        android:id="@+id/btn_favorite_recipe"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_favorite"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Favoritar Receita"
        android:visibility="gone"
        app:tint="@color/orange_primary"
        app:layout_constraintEnd_toEndOf="@id/tv_chat_message"
        app:layout_constraintTop_toBottomOf="@id/tv_chat_message"
        tools:visibility="visible"/>

</androidx.constraintlayout.widget.ConstraintLayout>